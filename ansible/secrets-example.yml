# Example secrets file: DO NOT commit your real secrets.
# Use ansible-vault to encrypt a secrets file, e.g.:
# ansible-vault create ansible/secrets.yml
# and then put values like:
# pihole_password: "<secure_password>"
# gui_token: "<random_token_for_gui>"

# Placeholder example:
# pihole_password: "BigBlock2534!"
# gui_token: "your-very-long-random-token"
